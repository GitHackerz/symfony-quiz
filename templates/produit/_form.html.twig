{{ form_start(form) }}
{{ form_widget(form._token) }}

<div class="flex items-center justify-center w-full">
    <div class="mb-5 bg-white dark:bg-gray-800 p-10 w-1/2 shadow rounded-xl">
        <h1 class="text-2xl font-semibold text-gray-900 dark:text-white text-center mb-5">{{ button_label }} un Produit</h1>
        <form class="max-w-sm mx-auto">
            <div class="mb-5">
                <label for="nom"
                       class="block mb-2 text-sm font-medium {{ form_errors(form.nom) ? 'text-red-700 dark:text-red-500' : 'text-gray-900 dark:text-white' }}">Nom</label>
                <input
                        name="{{ field_name(form.nom) }}"
                        value="{{ field_value(form.nom) }}"
                        type="text"
                        id="nom"
                        class="{{ form_errors(form.nom) ? 'bg-red-50 border border-red-500 text-red-900 placeholder-red-700 text-sm rounded-lg focus:ring-red-500 dark:bg-gray-700 focus:border-red-500 block w-full p-2.5 dark:text-red-500 dark:placeholder-red-500 dark:border-red-500' : 'shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500 dark:shadow-sm-light' }}"
                        placeholder="{{ field_label(form.nom) }}">
                <p class="mt-2 text-sm text-red-700 dark:text-red-500">{{ form_errors(form.nom) }}</p>
            </div>

            <div class="mb-5">
                <label for="image"
                       class="block mb-2 text-sm font-medium {{ form_errors(form.image) ? 'text-red-700 dark:text-red-500' : 'text-gray-900 dark:text-white' }}">Image</label>
                <input
                        name="{{ field_name(form.image) }}"

                        type="file"
                        id="image"
                        class="{{ form_errors(form.image) ? 'bg-red-50 border border-red-500 text-red-900 placeholder-red-700 text-sm rounded-lg focus:ring-red-500 dark:bg-gray-700 focus:border-red-500 block w-full p-2.5 dark:text-red-500 dark:placeholder-red-500 dark:border-red-500' : 'shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500 dark:shadow-sm-light' }}"
                >
                <p class="mt-2 text-sm text-red-700 dark:text-red-500">{{ form_errors(form.image) }}</p>
            </div>

            <div class="mb-5">
                <label for="description"
                       class="block mb-2 text-sm font-medium {{ form_errors(form.description) ? 'text-red-700 dark:text-red-500' : 'text-gray-900 dark:text-white' }}">description</label>
                <textarea
                        name="{{ field_name(form.description) }}"
                        id="description"
                        class="{{ form_errors(form.description) ? 'bg-red-50 border border-red-500 text-red-900 placeholder-red-700 text-sm rounded-lg focus:ring-red-500 dark:bg-gray-700 focus:border-red-500 block w-full p-2.5 dark:text-red-500 dark:placeholder-red-500 dark:border-red-500' : 'shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500 dark:shadow-sm-light' }}"
                        placeholder="{{ field_label(form.description) }}"
                >{{ field_value(form.description) }}</textarea>
                <p class="mt-2 text-sm text-red-700 dark:text-red-500">{{ form_errors(form.description) }}</p>
            </div>

            <div class="mb-5">
                <label for="matiere"
                       class="block mb-2 text-sm font-medium {{ form_errors(form.matiere) ? 'text-red-700 dark:text-red-500' : 'text-gray-900 dark:text-white' }}">Matiere</label>
                <input
                        name="{{ field_name(form.matiere) }}"
                        value="{{ field_value(form.matiere) }}"
                        type="text"
                        id="matiere"
                        class="{{ form_errors(form.matiere) ? 'bg-red-50 border border-red-500 text-red-900 placeholder-red-700 text-sm rounded-lg focus:ring-red-500 dark:bg-gray-700 focus:border-red-500 block w-full p-2.5 dark:text-red-500 dark:placeholder-red-500 dark:border-red-500' : 'shadow-sm bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500 dark:shadow-sm-light' }}"
                        placeholder="{{ field_label(form.matiere) }}">

                <p class="mt-2 text-sm text-red-700 dark:text-red-500">{{ form_errors(form.matiere) }}</p>
            </div>

            <div class="w-full flex items-center justify-center">
                <a href="{{ path('back_produit_index') }}"
                   class="text-white bg-gray-700 hover:bg-gray-800 focus:ring-4 focus:outline-none focus:ring-gray-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-gray-600 dark:hover:bg-gray-700 dark:focus:ring-gray-800 self-center mr-5">
                    Annuler
                </a>
                <button type="submit"
                        class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800 self-center">
                    {{ button_label }}
                </button>
            </div>

        </form>
    </div>
</div>

{{ form_end(form) }}
